# Security

Only load `.mat` files you trust. They can allocate a lot of memory. If a dataset publishes checksums, check them.

If you think you found a vulnerability, email the repo owner. Do not open a public issue with exploit details or participant data.

There is no stable release yet; fixes go to `main`.
